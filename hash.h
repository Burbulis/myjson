

#ifndef __HASHER__
#define __HASHER__
#include <math.h>
#include <stdio.h>

  unsigned long get_hash(unsigned long index);
  unsigned long rehasher(unsigned long x_,unsigned long count_,char token_);
  
#endif
